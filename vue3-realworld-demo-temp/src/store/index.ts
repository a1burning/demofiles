import { createStore } from 'vuex'

// 以前创建store实例 new Vuex.Store({ state: { xxx }})
// 现在使用createStore去创建
export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
